# Vue 3 + Element Plus 迁移清单

## 学生与审核
- 搜索学号、状态筛选、状态统计卡片
- 全选当前筛选结果；筛选变化清理历史选择
- 查看流程/审核：原图、成片、处理阶段、版本与操作记录
- 审核通过、退回、撤回审核、上一张、下一张、跳过
- 批量审核通过/退回，备注可空；仅处理待人工审核
- 预览处理计划；重新计算；开始处理
- 生成照片交付包；导出选中学号
- 异常：无选择、无可审核成片、修订号冲突、已交付保护

## 数据接入
- 导入学号名单并校验；按当前届次隔离
- 导出当前届全部学号 TXT；导出当前届已交付 TXT
- Excel 检查：自动识别学号列、照片列、学年；多学年拒绝；识别不到弹窗选择
- Excel 正式导入；已交付学生跳过
- ZIP 检查与导入；学号-姓名文件名校验；重复照片不重复建版本
- 批量上传图片
- 登记历史已交付名单；未登记学号自动加入
- 旧数据迁移：检查、复制

## 处理配置
- 通用预检：方向、黑白、人脸、反光、翻拍、失败停止
- 背景引擎：none、quick、ai、hivision
- 背景色、裁切尺寸、质量阈值、方向阈值
- Hivision 地址、超时、尺寸、高清、人脸对齐、抠图/人脸模型、DPI
- Hivision 历史地址：同一下拉框查看/切换；添加；右侧删除；测试连接（已实现；未运行验证）
- 保存配置、导入配置 JSON、导出配置 JSON

## 任务进度
- 任务状态、完成数、当前学号、失败数、错误与计划
- 暂停、继续未完成项、安全中断

## 照片交付
- 交付批次列表：待实际交付、已交付、已取消、历史批次
- 下载照片 ZIP；确认已交付；取消交付；查看学号
- 已交付后照片变化和替换流程

## 全局与状态
- 当前届次切换
- 工作区路径显示
- API 文档入口
- 检查更新
- 退出程序
- 启动鉴权：启动器 token -> `/session` -> Cookie；API 未授权返回 403

## 迁移验收
- Vue 页面不使用 iframe
- 构建产物只进入临时打包目录，不回写源码目录
- 新目录启动最终 EXE
- 鉴权、届次切换、导入→处理→审核→交付完整流程

## 当前核对状态（2026-09-14）
- 已实现：Vue 组件壳、统一导航、鉴权入口、届次切换、学生列表筛选/批量审核、导入入口、任务操作、交付列表操作、Hivision 地址添加/切换/删除。
- 已运行验证：Vue 生产构建通过；接口路由存在性已核对。
- 已实现：处理计划弹窗/开始处理、学生详情审核、照片替换、导入 Excel/ZIP 入口、任务操作、交付下载/确认/取消。
- 未完成：生成交付包按钮串联、单张试处理完整阶段结果、完整配置字段、Hivision 测试及配置导入导出、历史交付登记、旧数据迁移。
- 阻塞：候选 EXE 已启动但当前会话无法直接读取浏览器 UI 或执行逐页点击截图；因此完整 EXE 流程仍未验收。
- 阻塞：当前尚无业务阻塞；最终 EXE 验收需候选包生成后执行。

## 候选包验收记录
- 候选 ZIP：C:\Users\nivaya\Documents\Codex\2026-09-09\ni-xi\StudentPhotoFlow_Vue3_candidate_20260914.zip
- SHA-256：F26C96E6FE8AEAF4E7650D0F82D2DD28F9EA5EAEA6FA9AA7CBE77FCE34ADC7EC
- 解压目录启动：进程 StudentPhotoFlowV2 已存在；逐页 UI、导入→处理→审核→交付尚未完成。
