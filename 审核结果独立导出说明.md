# 审核结果独立导出脚本

适配 StudentPhotoFlow v1.8 的 `审核归档/学号/图片指纹.扩展名` 结构。
不修改主程序、现有照片或审核 JSON，也不会再次处理图片。

从 **StudentPhotoFlow v1.9** 起，同样的功能已合并到便携版主程序，点击 **“审核结果导出…”** 即可选择初次导入、新增或仅导出名单，无需单独安装 Python。
本文件下方的独立脚本和命令行用法继续可用。主程序结果网页的“全年级综合统计与学号”与本脚本共用统计逻辑。

## 只查“已处理但未审核通过”的综合学号

双击脚本选择 **“仅导出名单”**，再选择完整的源“导出结果”目录和结果保存目录。
无需逐个打开网页，也不会重新复制已通过照片。会新建 `名单查询_时间_随机后缀` 文件夹，主要看：

- `已处理但未审核通过学号.txt`：你要的综合名单，一行一个学号、全年级去重。
- `已处理但未审核通过明细.csv`：同一批学号的分类、人工审核状态、处理状态、错误原因、版本说明和依据文件；可在 Excel 中筛选。
- `未通过分类名单/`：每个分类一个 TXT，可直接取“人工审核不通过”“机器检查不通过”“处理失败”等名单。
- `未发现处理记录学号.txt`：其余未通过、但没有找到处理证据的学生，单独列出，不混入已处理名单。
- `名单查询提示.csv`：历史损坏、缺失、中断结果不完整等提示；有提示时不能将综合名单当成完全无遗漏。

原来的 `未审核通过学号.txt` 仍然保留，含义是“全年级名单减去当前有效审核通过”，包含尚未上传 / 导出 / 处理的学生，范围更大。
仅查询不会生成 `审核通过学号.txt` 或编号累计名单，不读取累计交付人数，不占用 `新增(数字)` 编号，也不影响下次新增导出。
“初次导入”和“新增”导出照片时也会附带以上综合名单，且都统计全体学生，不只统计本次新增学生。

```powershell
py "导出审核结果.py" "D:\StudentPhotoFlow\导出结果" --output "D:\名单查询" --lists-only
```

### 综合名单的统计规则

这里的“已处理”表示 **存在图片处理或处理尝试的记录**，包括执行失败，不等于成功生成成片。
脚本只读取本地数据，结合 `export_state.json`、当前审核快照，以及 `批次记录/*/batch.json`、`run_status.json` 和主状态里的断点记录：

1. 排除当前有效人工审核通过的学生；“机器处理成功”不等于“人工审核通过”。
2. 补查历史处理失败，避免主状态仍为 pending 或保留旧成功结果时漏人。
3. 同一学生只列一次，优先按记录时间确定最近处理结果；同一批次主状态的详细结果优先于粗略断点。不会按文件复制时间排序。
4. 主分类包括：已处理待审核、人工审核不通过、审核跳过、旧审核失效、机器检查不通过、处理失败、处理警告或异常、仅历史处理记录、处理结果待核实。
5. 人工决定优先显示在“分类”列；“最近处理分类 / 状态 / 处理说明”单独保留。例如人工跳过但 API 超时，两种情况都能从明细查到。API 超时属于程序记录的 warning，不会被当作学生照片不合格。
6. 当前原图已更新而处理记录属于旧版本的学生仍列入，但明确标注“仅历史版本”，不会说新照片已经处理成功。版本比对依据保存的输入指纹和导出时间；无法确认的明确标注待核实。
7. 未执行的计划项、已归档跳过、批次里复用的 unchanged 结果，不当作一次新处理。
8. 历史累计交付名单只控制照片去重，不控制综合名单：某学生以前导出过，后来审核失效或改成不通过，本次仍会列出。

**强制中断的限制**：v1.8 断点只保存明确完成的学号、失败总数和计划，并不保存全部失败详情。
没有 `batch.json` 时，只补查明确已完成的学号，不能把所有计划中 / 等待中的学生都当作已处理或失败。
未写入磁盘的结果无法从旧版数据恢复；此时输出提示并将 `history_scan_complete` 标为 `false`。
历史文件缺失、损坏、处理时间无法判断时也会提示，不会静默宣称统计完整。

各名单互不重复，且满足：当前有效审核通过人数 + 已处理但未通过人数 + 未发现处理记录人数 = 全年级人数。
“未发现处理记录”只表示在可读取数据里没有发现证据；有查询提示时不应直接解释为该生从未处理过。
这些补丁只生成名单，不在主网页添加按钮、不修改审核、不自动要求学生重传。

## 如何运行

脚本是 `export_reviewed_photos.py`，可以重命名为 `导出审核结果.py`，单独复制到任意目录。
需要 **Python 3.10 或以上版本**，只使用标准库，不需要安装 Pillow、OpenCV、AI 模型等依赖。
便携版的 `PhotoExporter.exe` 不是通用 Python，不能拿来运行这个 `.py` 文件。

1. 先结束或安全中断主程序中的导出、处理任务，并停止在网页里审核。建议直接关闭主程序。
2. 双击 `.py` 文件，或在终端执行 `py 导出审核结果.py`，选择“初次导入”“新增”或“仅导出名单”。
3. 选择源“导出结果”目录（也可以选择便携版根目录或其“审核归档”目录）。
4. 选择交付父目录。新增时选择与之前相同的父目录，便于自动找到历史名单；找不到时会提示选择历史 TXT。
5. 导出完成后查看数量及 `导出异常.csv`。旧目录、旧名单均保留，不覆盖。

如果没有 Python 文件关联，请用终端运行。如果系统没有 Tk 文件夹选择功能，会改为控制台输入路径。
也可以完全不使用窗口：

```powershell
# 初次导入：导出全部当前有效通过的照片
py "导出审核结果.py" "D:\StudentPhotoFlow\导出结果" --output "D:\照片交付" --mode initial

# 新增：自动识别交付父目录中的历史累计名单
py "导出审核结果.py" "D:\StudentPhotoFlow\导出结果" --output "D:\照片交付" --mode incremental

# 新增：手动指定旧版名单或某次累计名单，也可使用新的交付父目录
py "导出审核结果.py" "D:\StudentPhotoFlow\导出结果" --output "D:\照片交付" --mode incremental --previous "D:\上次结果\审核通过学号.txt"
```

仅指定源目录时，默认在源目录的上一级创建 `审核交付导出`。
参数 `--no-gui` 强制使用控制台；`--help` 查看参数。传入源路径但未指定 `--mode` 时默认初次导入，保持旧命令兼容。
`--lists-only` 表示仅查询名单，不需要历史累计名单，不能与 `--previous` 合用。

## 两种模式和累计名单

- **初次导入**：不看历史跳过名单，导出当前有效通过的所有照片到一个新的 `初次导入_时间_随机后缀` 目录。
  其中 `审核通过学号.txt` 记录本次成功导出照片的学号，作为新增的起点。
- **新增**：读取历史 `审核通过学号.txt` 或 `审核通过学号(数字).txt`，名单内的学号全部跳过。
  只把当前有效通过、但未在历史名单中的学生照片导出到 `新增(1)`、`新增(2)`……，图片直接放在该编号文件夹内。
  每次新建相同编号的 `审核通过学号(数字).txt`，内容为 **旧累计名单 + 本次成功导出的学号**，不会覆盖旧文件。

自动识别范围是所选交付父目录本身，以及其下一层 `审核交付_...`（旧版）、`初次导入_...`、`新增(数字)` 目录。
有编号的名单优先采用最大数字（10 大于 9），没有编号时选择最近保存的初次名单。
同一编号存在不同内容时会要求明确选择；未完成、失败批次的名单不会自动采用。
建议每个年级使用独立的交付父目录。名单被移动到其他位置时，用 `--previous` 指定，或在窗口提示时选择。

新增编号取已有文件夹 / 同级名单和所选历史名单的最大编号加一；失败批次占用的编号不复用。
没有新增照片时，也会生成空的编号批次和累计名单副本，并明确报告“本次导出 0 张”。
新增是按学号去重，不是按照片内容去重：已在历史名单中的学生，即使重传并再次通过，新增模式也不会重复导出；需要全部重导时用初次导入。

旧版本脚本生成的 `审核通过学号.txt` 仍能直接读取。旧版该文件可能包含审核通过但照片复制失败的学生，
使用旧名单前请核对对应的 `导出异常.csv`。新版本累计名单只添加复制成功的学号，失败项修复后可在下次新增重试。

## 会导出什么

```text
照片交付/
├─ 初次导入_时间_随机后缀/
│  ├─ 审核通过照片/2602210081.jpg
│  ├─ 审核通过学号.txt               初次成功导出的名单
│  └─ …明细及摘要
├─ 新增(1)/
│  ├─ 2602210082.jpg                只有第一次新增的照片
│  ├─ 审核通过学号(1).txt           初次名单 + 第一次新增
│  ├─ 本次导出学号.txt
│  ├─ 当前有效审核通过学号.txt
│  ├─ 未审核通过学号.txt
│  ├─ 未审核通过明细.csv
│  ├─ 已处理但未审核通过学号.txt
│  ├─ 已处理但未审核通过明细.csv
│  ├─ 未通过分类名单/
│  ├─ 未发现处理记录学号.txt
│  ├─ 未发现处理记录明细.csv
│  ├─ 名单查询提示.csv
│  ├─ 已导出照片明细.csv
│  ├─ 导出异常.csv
│  └─ 导出摘要.json
└─ 新增(2)/
   ├─ 2602210083.png                只有第二次新增的照片
   ├─ 审核通过学号(2).txt           上次累计名单 + 第二次新增
   └─ …明细及摘要
```

- **照片按学号平铺命名**，不再每人一个文件夹；保留原扩展名和完整原始字节，不压缩、不改尺寸。
- `审核通过学号[编号].txt` 是用于去重的累计交付名单；`当前有效审核通过学号.txt` 才是本次检查时全部有效审核通过的学生；`本次导出学号.txt` 只列本次成功复制的学生。
- **其余学号 = 已保存全年级名单 − 当前有效审核通过项**，不是仅统计已经采集的照片。尚未上传、未导出、未处理、未审核、审核不通过、跳过、旧审核已失效的学生都会列入。顺序与全年级名单一致。
- 以 `review_state.json` 的当前标记为准，使用与 v1.8 相同的照片及流程快照校验；归档目录中留存的旧副本、撤销前副本不会误导出。
- 审核开关是否开启不影响导出已有有效通过项；但名单必须是主程序校验保存的完整名单。名单外的记录不混入交付，在摘要中列出。
- 审核仍有效但归档文件丢失或指纹不匹配时，单独记录导出异常，**不会把已通过学号混入未通过名单**。此时“已导出照片数”可能少于“审核通过人数”，请处理异常后重新导出。
- 如果原图、成片、流程时间或图片文件时间戳改变，与 v1.8 一样会视为旧审核失效，列入需重新审核。
- 不会发送重传通知，也不会修改任何审核标记。

## 数据与异常说明

源目录必须保留同一份输出数据的 `grade_roster.json`、`export_state.json`，以及已有的 `review_state.json` 和对应原图、成片、归档副本。
刚保存名单、尚未做过人工审核时可以没有 `review_state.json`，按无人审核处理，不会创建源文件；已有审核数据请勿漏复制该文件。
只有“审核归档”文件夹无法判断撤销状态，也无法算出全年级其余学生，脚本会拒绝猜测。
补查综合名单还需要保留原 `批次记录` 目录及其中的 JSON；不要只挑出最新批次。
仅查询模式不会复制或检查归档副本，但仍需要原图、成片及三份主 JSON 来核对审核是否有效。

TXT 使用 UTF-8 BOM，一行一个学号、没有表头，前导零不丢失。
CSV 使用 UTF-8 BOM；有前导零的学号如需在 Excel 中使用，请按文本列导入，或使用 TXT 名单。

脚本会尽量取得主程序已有的目录锁，并核对导出前后的主 JSON 与历史批次 JSON 内容。发现源状态改变时，本次标为失败，避免交付混合版本的数据。
`导出摘要.json` 的 `status`：

- `completed`：完成且没有照片导出异常或名单查询提示。
- `completed_with_errors`：部分已通过照片导出失败，或历史补查存在不完整 / 待核实情况；分别看 `导出异常.csv` 和 `名单查询提示.csv`。
- `running` / `failed`：未完成或失败，**不要交付该目录或使用里面的累计名单**。关闭程序后重新运行会生成新目录，不覆盖旧目录。

命令行退出码：`0` 成功或取消目录选择；`1` 失败；`2` 完成但有照片导出异常或名单查询提示。
