# 2.4.1 任务时间与 Hivision 并发

基线：正式 v2.4.0；功能分支 feature/task-timing-hivision-concurrency。
最终应用/镜像源码：9b7d3c077814b376ea1802834103e71a7d5b60a4。后续仅补充验收文档，不移动版本标签。
候选：v2.4.1-rc.2；Registry digest：sha256:d834c4c655491ff3e80783e02fd6f28f76e85bda843df3fe9a778f7ddff9466e。

## 已完成

- [x] 核对正式版基线、配置、统一任务模型、分届服务及HTTP调用路径；独立测试工作区。
- [x] 开始/结束/总历时/实际运行时长，接收/处理/打包统一持久化；暂停收尾计时；异常检查点恢复；旧任务缺失/部分计时标识。
- [x] 后端原子读取计时快照，避免轮询与计时心跳竞争；前端动态显示及终态固定。
- [x] 并发1–16整数，旧配置缺省1，保存读取、配置JSON兼容、前后端校验。
- [x] 同一实例服务地址共享限额，跨届任务、单人、单张试处理复用；动态降限仅约束后续调度。
- [x] 并发按学生/届次写回，暂停/安全结束停止新调度，在途结果收尾，重复保护和终态校验。
- [x] HTTP503/非法JSON/超时释放名额，不无限重试；模拟服务峰值和调用时间证据。
- [x] 源码环境59测试通过；最终镜像核心38测试通过（桌面Tk/review_web测试不计最终Linux通过）。
- [x] 最终Docker接口验收：串行峰值1、共享峰值3、降至1、暂停/继续、手动终态、kill/remove/recreate确认下限。
- [x] v2.4.0有数据副本升级：323学生、10任务、6交付、1回收站；业务字段、照片哈希保留。
- [x] 最终独立Chromium：配置保存/导入导出、接收→计划→处理→审核→交付；4张JPG数量/学号正确。
- [x] 单人弹窗与跨届批量并行峰值2，关闭再打开、并发安全结束、重复结果跳过。
- [x] 容器实际重建，128文件哈希和业务字段一致；暂停时间、配置保留。
- [x] GHCR原始最终镜像上传；隔离无凭据匿名拉取；拉取镜像登录/版本/计时/交付下载/退出验证。
- [x] 12张真实页面截图、模拟统计、完整验收记录和匹配版本的部署ZIP，无业务数据或密钥。
- [x] 推送功能分支及新标签；GitHub Pre-release创建成功，8个附件服务端SHA核对通过。
- [x] 回下载部署ZIP逐字节一致；Compose解析确认版本、端口、数据挂载；最新正式版仍为v2.4.0。
- [x] main未修改；2.4.0/stable/latest仍指向原正式镜像digest。

## 已知限制 / 未验证

- 真实Hivision性能及人像质量本轮未验证；只用合成照片/模拟服务，无生产调用。
- 崩溃运行时长为持久化确认下限；缺历史不补造，旧任务恢复只记部分时长。
- 单进程单副本；同届仍一次一个正式任务；不同URL别名不自动合并限额。
- HTTP超时不能取消远端后台计算；无自动重试。Linux amd64实测，Windows桌面及其他架构未验收。
- 继承正式版网页仅检查正式Release的限制，候选升级使用固定标签。
- rc.1为过早推送的不完整中间点，保留旧标签/镜像，不创建其Release；本次完整交付rc.2。

进行中：无。未完成的本轮必需项：无。外部阻塞：无。
报告：RELEASE_241_VALIDATION.md。
发布：https://github.com/HankNovic/StudentPhotoFlow/releases/tag/v2.4.1-rc.2
