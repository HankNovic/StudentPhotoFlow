# StudentPhotoFlow V2

完整解压便携包，双击 StudentPhotoFlowV2.exe，浏览器会自动打开工作台。程序数据默认保存在旁边的 V2工作区。退出浏览器不会中断后台任务；从任务页安全中断后点击侧栏“退出程序”。

## 开始使用

1. 数据接入：粘贴全年级学号，一行一个；或者导入Excel，先检查列设置，再接收原图。批量图片上传以文件名匹配学号。
2. 处理配置：选择内置AI或Hivision，保存配置；可导入、导出JSON配置。默认不换背景。
3. 学生与审核：筛选、选中学生，点击“预览处理计划”，核对执行/跳过原因，再运行。
4. 详情中比较原图和成片；点击照片看原尺寸；“试处理”只生成预览，不更改当前审核。
5. 审核通过后，选择学生生成交付包。包内图片为学号命名。
6. 真正发送给接收方后，点击“确认这批照片已经实际交付”。同一学生不会再次进入普通交付。

已交付后收到新原图会进入“已交付后照片变化”；填写原因启动替换流程后，可生成新版本。旧交付记录保留。
修改配置不自动处理旧照片。显式勾选“生成新处理版本”可以重做已通过照片；新结果需重新审核。
暂停/中断在当前学生处理结束后生效。意外关闭时下次启动可继续未完成项，失败项通过新任务重试。

## 旧版迁移

使用新的空V2工作区，在数据接入输入旧“导出结果”目录，先检查再迁移。旧目录不修改。
历史已交付名单转换为历史交付批次，未确认的成片版本不自动通过。旧审核只有原图、结果、归档文件内容哈希同时匹配才继承。
之前实际发送的483人名单，可在V2“数据接入”的历史交付区域粘贴学号，填写依据并确认已实际发送。此操作防止重复交付，不会冒充当前照片已审核通过；也可通过历史交付名单API录入。

## API

工作台侧栏提供交互式OpenAPI文档。图片上传为multipart，其他写接口使用JSON；学号一律字符串。
外部程序使用 Authorization: Bearer <令牌>。通过环境变量 SPF_API_TOKEN 设置固定令牌，并启动：

```
StudentPhotoFlowV2.exe --workspace D:\照片工作区 --host 127.0.0.1 --port 8765 --no-browser
```

需要局域网接入时指定 --host 0.0.0.0，并设置令牌。默认不会公开到局域网。
调用 POST /api/v1/processing-jobs 时先传 dry_run:true 获取执行计划；再携带 expected_revision 提交。任务状态查询 /api/v1/jobs；SSE /api/v1/events 提供变更通知。
交付分生成、下载、确认三个动作。下载不代表已实际交付。

## 数据与运行边界

业务事实集中在 workspace.json，一次事务原子保存；照片按内容哈希存储于 artifacts，交付包是学号命名。复制整个工作区即可迁移。不要只复制JSON。
单个工作区只允许一个程序进程写入；处理任务串行运行。可在处理其他学生时审核完成项。
当前复用已有内置和Hivision图像算法；Hivision内部不暴露的步骤不会伪造中间预览。
Excel导入作为后台任务逐项接收，可暂停和中断；已接收原图即时保存，重新导入相同内容自动去重。恢复时若原链接过期，需要导入新的Excel。

## V2.0.2 更新
- Hivision 服务区块内可添加、切换历史 API 地址及测试当前地址；历史地址随工作区保存。切换后保存配置以供下次运行使用。
- 历史已交付名单可直接登记，自动补入缺少的学号；无效名单整批不保存。
- 检查 Excel 时每次重新识别列，未识别默认 A/B；正式导入严格使用界面所选列。

## V2.0.3 更新
数据接入新增照片压缩包入口。ZIP 名称不限，内部文件为 学号-姓名.图片扩展名，可放在子目录内。先检查，再接收原图；自动补入学号，重复照片不重复建立版本。任务支持暂停、继续、安全中断；损坏图片会记录为单项失败。最多500MB，解压总大小2GB，单张30MB。

