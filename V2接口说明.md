# V2 中文 API 使用说明

学生照片工作台的本地接口。所有学号使用字符串，保留前导零。

### 认证与错误
从启动器打开工作台后，浏览器使用会话 Cookie。外部程序在每次请求中添加 `Authorization: Bearer <令牌>`；可在启动前设置环境变量 `SPF_API_TOKEN`。端口由启动器分配，固定端口可用 `--port 8769`。

JSON 请求使用 `Content-Type: application/json`；上传文件使用 multipart/form-data。
403：认证失败或跨站访问；404：记录不存在；409：业务状态冲突或配置非法（message 为中文原因）；422：请求字段验证失败（detail 为字段错误列表）。遇到修订号冲突应刷新记录，检查后重试。

### 推荐调用顺序
登记名单 → 上传原图 → 预览处理计划 → 正式处理 → 查询任务 → 查询学生详情 → 审核成片 → 生成交付包 → 下载 → 实际发送后确认交付。

预览计划示例：`{"student_ids":["001"],"config":{"crop_enabled":true,"crop_width":295,"crop_height":413},"dry_run":true}`。
正式处理复用同一份 student_ids 和 config，设置 dry_run=false，并传入预览返回的 revision 作为 expected_revision。
审核示例：`{"student_id":"001","result_id":"从详情取得的版本ID","decision":"approved","expected_revision":3}`，修订号也应从当前详情取得。

### 学生状态
missing 未采集；pending 原图待处理；processing 任务未完成；review 待人工审核；approved 审核通过待交付；delivered 已交付；delivered_updated 交付后照片变化；machine_rejected 机器退回；review_rejected 人工退回；failed 处理失败。

### 处理配置参数
GET /config 返回每项默认值及已保存值。配置和单张预览接收相同字段；background_mode 为 none（不处理）、quick（快速换色）、ai（本地抠图）或 hivision（外部服务）。颜色使用 #RRGGBB，宽高单位为像素；布尔值使用 true/false。Hivision 参数仅在该引擎下生效；最终裁切在背景处理之后执行。

| 字段 | 中文含义 |
| --- | --- |
| `quality_enabled` | 启用内置预检 |
| `auto_orient` | 校正90/180/270度方向 |
| `check_grayscale` | 黑白检查 |
| `check_face` | 人脸检查 |
| `check_glare` | 反光检查 |
| `check_recapture` | 翻拍检查 |
| `background_mode` | 图像处理引擎 |
| `background_color` | 背景颜色 |
| `crop_enabled` | 最终裁切并缩放 |
| `crop_width` | 最终宽度（像素） |
| `crop_height` | 最终高度（像素） |
| `hivision_url` | Hivision API地址 |
| `hivision_timeout` | API超时（秒） |
| `hivision_width` | Hivision标准图宽 |
| `hivision_height` | Hivision标准图高 |
| `hivision_hd` | 使用高清返回 |
| `hivision_face_align` | Hivision小角度对齐 |
| `hivision_matting_model` | Hivision抠图模型 |
| `hivision_face_model` | Hivision人脸模型 |
| `hivision_dpi` | 打印DPI |
| `stop_on_reject` | 预检不通过时停止 |
| `grayscale_ratio_threshold` | 黑白比例阈值 |
| `grayscale_delta_limit` | 灰度色差阈值 |
| `face_confidence_threshold` | 人脸置信度阈值 |
| `glare_ratio_threshold` | 反光比例阈值 |
| `glare_luma_threshold` | 过曝亮度阈值 |
| `recapture_score_threshold` | 翻拍分数阈值 |
| `orientation_min_confidence` | 方向最低置信度 |
| `orientation_confidence_margin` | 旋转置信度优势 |
| `hivision_head_measure_ratio` | 面部占比 |
| `hivision_head_height_ratio` | 面部中心高度 |
| `hivision_top_distance_max` | 头顶留白最大比例 |
| `hivision_top_distance_min` | 头顶留白最小比例 |
| `hivision_brightness_strength` | 亮度调整 |
| `hivision_contrast_strength` | 对比度调整 |
| `hivision_sharpen_strength` | 锐化调整 |
| `hivision_saturation_strength` | 饱和度调整 |


## 建立浏览器会话

提交 JSON：`{"token":"访问令牌"}`。成功返回 ok 并设置 HttpOnly 会话 Cookie。令牌错误返回 403。

## 查看版本和工作区

返回 version（程序版本）及 workspace（当前工作区绝对路径）。

## 安全退出程序

返回 ok；仍有运行任务时返回 409，应先中断任务并等待停止。

## 查询学生列表

status 为状态代码，留空不过滤；q 按学号包含匹配。返回数组，每项含 id、status、source、result、revision。状态代码见上方说明。

## 查询学生完整记录

sid 为学号（保留前导零）。返回 sources 原图版本、results 处理版本、history 操作记录、delivered 交付事实及 revision。不存在返回 404。

## 追加学号名单

提交 student_ids 字符串数组，例如 {"student_ids":["001","002"]}。已有学号不会重复添加。返回名单登记结果。

## 上传学生原始照片

sid 必须已登记。使用 multipart/form-data，文件字段必须名为 photo，最大 30MB。同一原图重复上传不会清除审核记录；新原图保留旧版本。返回原图信息。

## 读取默认和已保存配置

返回 defaults 和 saved 两个对象。配置项中文含义见下方参数对照。

## 校验并保存处理配置

提交配置 JSON 对象，返回完整配置。未提供的字段使用系统默认值，不是与旧配置合并。未知字段、错误类型或非法取值返回 409。保存不自动重做照片。

## 测试 Hivision 服务

提交 {"url":"http://127.0.0.1:8080"}。只检查接口能力，不上传照片；返回服务检测结果。

## 预览计划或启动批处理

dry_run=true 返回 items、config、config_id、revision；items 的 execute 表示是否执行，reason 解释跳过原因。正式执行设置 dry_run=false，并把计划 revision 传入 expected_revision。返回任务记录，使用 GET /jobs 跟踪。默认跳过已通过、已交付及相同参数已有结果的照片。

## 试处理单张照片

sid 为学号；请求体为配置对象（同 PUT /config）。返回 status、stages、reasons、artifact。试处理写入预览图片，但不成为正式成片，不改变审核状态。没有原图返回 409。

## 查看任务进度

返回任务数组，最新在前；id 为任务标识，status 为状态，completed 为已完成学号，errors 为失败明细，current 为当前学号。completed 状态表示任务结束，不代表每张均成功。

## 暂停、恢复或中断任务

job_id 为任务标识；action 可填 pause（暂停）、resume（继续未完成项）、cancel（安全中断）。暂停和中断在当前学生结束后生效。返回任务记录；不允许的操作返回 409。

## 审核具体成片版本

decision：approved 通过、rejected 退回、pending 撤回审核。result_id 必须是当前成片版本；expected_revision 使用学生详情中的 revision，冲突返回 409，刷新后重试。返回更新后的学生记录。

## 启动照片替换流程

sid 为学号；提交 {"reason":"更换照片"}。保留历史交付事实，允许后续重新处理。未结束任务或待交付包会阻止替换。返回 ok。

## 查询交付批次

返回批次数组：prepared 待交付、delivered 已实际交付、cancelled 已取消。historical=true 为历史登记批次，不提供照片包下载。

## 生成照片交付包

提交 student_ids。仅接收审核通过且不在其他待交付包中的学生。返回 prepared 批次及 id；此操作尚未标记实际交付。ZIP 内含学号命名照片与 manifest.json。

## 登记历史已交付名单

提交学号名单、交付依据 reason，并设置 confirmed_sent=true。只记录历史交付事实，不把当前成片标为审核通过。返回历史批次；缺少的学号自动加入名单；校验失败时整批不保存。

## 确认照片已经实际交付

batch_id 为交付批次。请在真实发送照片之后调用，返回更新后的批次。重复确认已交付批次返回原批次。

## 取消尚未交付的包

batch_id 为批次标识。仅 prepared 批次可取消，成功返回 ok；已交付批次返回 409。

## 下载交付照片 ZIP

batch_id 为非历史批次标识。返回二进制 ZIP 附件；已取消批次返回 409。

## 读取原图或处理图片

sha 为图片内容的 64 位 SHA-256 标识（artifact.id）。返回图片二进制，使用响应 Content-Type 判断格式。

## 订阅工作区变化

返回 text/event-stream 长连接。数据形如 data: {"revision":12}，表示工作区修订号变化；收到后重新查询学生或任务接口。

## 检查 Excel 或创建原图接收任务

multipart/form-data：file 为 .xlsx（最大 50MB）；sheet 留空取首张；header_row 表头行从 1 开始；id_column 学号列默认 A；image_column 图片列默认 B。inspect_only=true 每次按表头重新识别列，未识别默认 A/B；false 严格使用所选列创建导入任务。照片链接须仍有效。

## 检查或迁移旧版工作区

提交 {"path":"D:\\旧版\\导出结果","apply":false} 先检查；apply=true 才复制迁移，目标须符合空工作区要求。path 是运行服务的电脑路径。返回迁移报告；不覆盖旧数据。