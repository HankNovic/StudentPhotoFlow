# StudentPhotoFlow 2.4.0 正式发布验证

## 来源与变更边界

用户已试用并认可的基线：v2.4.0-rc.8，源码 c4d41c606dee0e4620562e0ebdf3da26a7713646，候选仓库digest sha256:60dd8b1ac789329fbc443ad333fcb0fe9e556248d23ff547298afcc5171ad33e。
main 从32f47a0b77f24ee181b7334cf290238c9a7f9e59快进包含功能分支全部历史，无冲突。正式镜像来源提交 dca895e968626ac9fb38c6e865a929cdee0111d5。

相对rc.8，业务后端和Vue页面源码无差异；变化仅包括正式版本号、Compose/.env/部署说明、前端build-info版本与提交信息、Docker发布配置及依赖约束文件。首次重建发现tifffile、urllib3会被重新解析为新版，最终已锁定到rc.8的完整Python依赖集合，逐项freeze比对相同。没有悄然升级业务依赖。

构建从已提交Git archive导出的隔离源码目录执行，不使用未提交文件。应用版本2.4.0、前端build-info.version=2.4.0、前后端build_id/源码提交均匹配实际构建提交。未把旧候选重新贴正式版本标签。

## 正式镜像验证

|验证项|结果及范围|
|---|---|
|版本、健康、正常鉴权|正式容器登录通过，页面、healthz、build-info版本及提交一致；健康healthy|
|rc.8数据副本升级|真实rc.8镜像先读取独立有数据副本，正常登录后停机备份；正式镜像重建后逐项比对通过|
|数据内容|升级前21名学生、8个任务、5条交付、1条回收站；完整JSON业务字段、配置、名单、审核、任务状态/不确定项/结束原因、照片及文件哈希相同；仅忽略新增启动审计和修订计数|
|后端/接口|正式镜像内7项反馈、9项系统、15项原有业务测试，共31项通过；没有覆盖镜像源码。测试工具在一次性测试容器中安装|
|更新弹窗|模拟未来2.4.1响应，文案“发现新版本2.4.1，是否查看？”，查看发布页打开指定URL；和网页构建刷新提示独立|
|失败详情|复用历史模拟503任务，3条错误默认展开；收起后等待多次轮询仍收起，展开恢复全部记录|
|ZIP接收|无效ZIP页面和直接接收接口均拒绝且未写学生；有效ZIP先检查不入库，明确确认后接收2张原图|
|计划分页|302条计划、执行2/跳过300；切换20/50/100展示并重新计算后正式任务仍含302条，仅处理2个有效原图，不改变执行范围|
|关键流程|页面ZIP接收→计划→本地处理→批量审核→生成交付包→下载→确认交付通过；ZIP含00101.jpg、00102.jpg及manifest.json，两张可读取300×420JPG|
|勾选项复核|批量计划中已移除，正常处理/交付保护沿用rc.8；单人弹窗仍残留一个无绑定的“明确生成新处理版本”勾选框，不影响请求参数，本次如实保留该已存在显示问题|

部分浏览器脚本最初因Element Plus中文分页标签、隐藏input定位及页面轮询时序未完成；按实际UI修正测试定位后继续验证，没有为测试更改业务代码。测试失败截图不作为通过证据。

## 已知限制与证据边界

这不是对rc.8功能重新设计。单人弹窗残留勾选框如上所述；分页在100条时仍沿用rc.8的弹窗布局，未新增固定高度/强制回到第一页逻辑，不将它宣称为新增修复。ZIP校验范围沿用rc.8既有规则，没有在正式发布阶段扩展导入规则。

本次处理冒烟使用合成图形照片和真实本地处理引擎；错误展示复用历史模拟Hivision错误记录，更新弹窗未来版本响应为受控模拟。没有调用生产Hivision，不代表真实人像抠图或人工质量再次验收。用户的rc.8实际试用结论作为业务基线保留。

无数据结构变更；不重置已有工作区。保持单进程、单写入实例；容器重建后会话失效须重新登录，中断结果仍需明确核对。旧任务终态不会因升级恢复。rc.3及更早目录不支持直接使用，此限制不适用于rc.8。

仅Windows Docker Desktop的Linux amd64容器实测；未宣称arm64或其他发行版逐项验证。不得让多个容器同时写同一数据目录。

升级、备份和回退准确命令见配套DOCKER部署.md。升级保留项目名、挂载路径、端口和口令；回退恢复到另一个目录，原目录/备份均保留，不将换镜像等同数据回退。

## 发布后核对

正式镜像：ghcr.io/hanknovic/studentphotoflow:2.4.0。
Registry digest：sha256:5a394a3d4d1fd5b49a09ed1ffa0cc676d3c3275c45fdc2fb11d0920ec3f8c792。已从远端查询并用空Docker凭据目录匿名拉取，2.4.0/stable/latest三个远端标签完全一致。
本机Docker push两次提交镜像层超时；备用Actions构建完成但推送被packages写权限拒绝。最终使用regctl经宿主机网络上传原已验证镜像，未采用Actions重建产物。上传前后manifest digest完全一致，没有重新构建或改变产物。
从仓库digest启动独立容器，健康healthy，正常登录/退出、页面和API版本2.4.0、源码提交dca895e、Notion交付页、已有合成交付包下载及Docker关闭服务接口禁用均通过。
正式Release https://github.com/HankNovic/StudentPhotoFlow/releases/tag/v2.4.0 已创建，draft=false、prerelease=false，GitHub /releases/latest 返回v2.4.0。部署ZIP从Release重新下载，SHA-256相同；解压后Compose配置核对固定镜像2.4.0、容器端口8769、/data挂载及/data/workspace均通过。
实际容器GitHub更新检查：直连返回HTTP403 rate limit exceeded，应用显示“暂时无法检查更新，请稍后重试”，业务仍可使用。这是本机出口真实限流，不将其记录为真实更新成功；未来版本弹窗成功验证仍属于模拟。带认证的GitHub CLI独立确认正式latest、发布说明及附件链接正确。
Actions运行35069212052的镜像推送失败（write_package）；正式镜像来自本机上传，不声称Actions发布成功。main后续工作流/报告提交不改变正式Git标签或镜像来源提交。

最终附件核对：8个资产的GitHub返回SHA-256全部与本地一致，部署ZIP已实际下载和检查。完整报告及截图在正式Release附件中。
