# 届次、回收站与单人补录任务清单

基线 v2.4.0-rc.3；继承 ce590cc。功能分支 feature/cohorts-recycle-single。候选 2.4.0-rc.4。
代码实现、接口验证、浏览器实测、最终镜像验收分别记录。进行中不等于通过。

|需求|实现|接口验证|浏览器/容器验证|
|---|---|---|---|
|固定 ID；改年份关联不变；同届学号唯一与前导零|完成|7 项系统测试通过|开发镜像实测通过|
|标签添加/编辑/Enter/Esc/统一保存/重复非法校验|完成|通过|添加/改名/保存/非法重复通过；Esc 最终补查|
|归档恢复/只读/空届删除/业务及回收站阻塞/无届引导|完成|通过|开发镜像实测通过|
|单人/批量/整届回收、范围确认、活动任务/共享交付保护|完成|通过|单人、批量共享、整届清空通过；活动任务为接口验证|
|完整恢复/冲突/永久删除/共享文件及试处理文件保护|完成|通过；新增重试引用检查待运行|恢复、冲突、明确永久删除通过|
|30 天/禁用/新设置仅影响新删除/启动补清理|完成|受控时间通过|页面关闭通过；容器受控到期补清理通过|
|中断恢复/失败重试/会话身份审计/记录持久化|完成|中断模拟与审计通过|容器重建记录/文件哈希/到期时间通过|
|精确单人补录/缺照行/详情入口/固定届次|完成|精确 ID 与隔离通过|行入口、顶部入口、找不到拒绝通过；跨侧栏最终补查|
|已保存全局参数、本次参数、正式单人任务/防重|完成|快照及重复请求通过|正式任务通过；默认已保存参数发现并修复，最终复查|
|关闭重开/进度/原图成片阶段/退回/版本及替换|完成|通过|开发镜像实测通过|
|单人及替换交付包下载/确认/保留历史|完成|通过|开发镜像实测通过，最终 ZIP 内容复查|
|基础批量导入处理审核交付及历史登记回归|完成|15 项原业务测试通过|批量完整流程、历史登记通过|
|最终镜像重建/清理/Notion 截图/真实素材隔离|进行中|开发镜像通过|最终镜像待构建实测；仅合成素材|
|候选版本/Compose/env/部署说明及旧目录保护|完成|待最终 Compose 实测|旧 schema 明确拒绝，无迁移承诺|
|源码敏感文件检查/提交/推送功能分支|进行中|不适用|未推送|
|同一镜像 GHCR rc 上传/匿名拉取/预发布附件|未完成|不适用|未发布|

确切外部阻塞：无。

实现方式：固定 ID 目录复用已有 Store/Service；全局处理配置不分叉。旧顶层 workspace.json 拒绝打开，要求新目录。回收站用原子 JSON 变更归属保留不可变文件；永久清理持久化待删除清单，可重试且每次重新核对共享引用。恢复不重跑任务。

数据结构取舍已经用户授权：schema 3 不兼容旧根工作区，不迁移、不覆盖；旧标签/镜像/数据原样保留。人工照片视觉质量及真实 Hivision 不计为本次通过，本次使用合成图和本地快速处理，未调用生产服务。

已发现并修正：单人详情应读已保存配置；接收中的手动上传和跨届恢复任务并发保护；设置保存中阻止标签竞态；试处理文件引用纳入回收；清理重试重新检查活跃引用。
测试脚本定位失败（Element Plus 隐藏 input、学号和照片标题同名）单独修正，不作为产品业务失败。

下一步：提交本地候选源码，构建带实际提交号镜像；在空目录运行三份 Chromium 脚本、8 项系统测试和15项回归，重建持久化及旧结构拒绝验证；通过后推送同一镜像并匿名拉取验证，更新最终报告。
