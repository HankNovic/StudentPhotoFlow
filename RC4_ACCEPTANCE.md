# StudentPhotoFlow v2.4.0-rc.4 验收记录

本版为 Docker 候选版，供新目录部署试用；不宣称所有生产场景已验收。保留 Vue 3 + Element Plus 与 Notion 风格。

## 发布产物

- 源码提交：24d2ed873884f21b02bad0b697cd1f742e3d6d87
- 功能分支：feature/cohorts-recycle-single
- 镜像：ghcr.io/hanknovic/studentphotoflow:2.4.0-rc.4
- **GHCR registry digest**：sha256:3ccd214810fd83cd56e523f593c54ae2db85795c0bf29361449134fb5c7e750c
- 本地 image ID：sha256:3ccd214810fd83cd56e523f593c54ae2db85795c0bf29361449134fb5c7e750c
- 此 Docker Desktop 存储下两值相同；registry digest 由实际 push 和独立匿名 pull 输出确认，并非拿本地 ID 代替。
- 镜像 OCI 标签、/healthz 的实际版本/源码提交均与上面一致。上传本地最终验收的同一镜像，未用 Actions 产物替换。
- Actions 只构建校验成功：https://github.com/HankNovic/StudentPhotoFlow/actions/runs/34977548242

## 验证方法与结果

本轮独立工作区、独立 Chromium 1440×1000，访问容器真实服务。正常管理员口令→session→HttpOnly Cookie；未关闭鉴权。照片为几何合成图，学号00001等均为测试值。未访问生产学生数据，未调用生产 Hivision。

1. **代码已实现**：固定ID届次管理、归档恢复、数据保护、回收站完整恢复/永久清理/保留时间、单人正式任务和参数快照、照片替换、审核和单人交付；全部复用现有Store/Service和处理组件。
2. **接口验证通过**：9项系统测试，包括届次/校验/跨届同号、活动任务与重复请求、恢复冲突、共享文件、保留期、受控到期、清理中断/重试、新增引用保护、单次参数快照、审计、会话退出和旧结构拒绝。原有15项业务回归通过。已交付直接上传测试按需求改为先启动替换，不是放宽断言。
3. **浏览器实测通过**：新增2027→改名2028关联不变；非法重复拒绝；Esc取消；归档恢复；空届删除确认/有业务拒绝；名单导入；单人上传→计划→处理→关闭重开→结果阶段→退回/通过→生成ZIP→下载→确认；已交付照片替换再次处理/审核/交付并保留历史；批量上传/处理/审核/交付；共享关联回收/恢复、整届清空/恢复、永久删除；同号恢复冲突；关闭自动清理；历史交付登记；配置保存/导出。
4. **本次参数验证通过**：已保存裁切宽度321，处理配置页面未保存草稿改999；单人弹窗仍读取321。弹窗本次改128，真实本地试处理返回128像素成片，关闭重开恢复321，不覆盖全局配置。
5. **下载核对通过**：原交付与替换交付ZIP各含一张00001.jpg及manifest.json；图像可解码，字节分别等于对应交付记录指向的成片。下载不改变交付状态，后续显式确认留有记录。未交照片的00004保持未采集、没有成片或交付；00005仅用于明确的历史交付登记测试。
6. **容器重建通过**：备份测试目录，删除的仅为脚本创建的测试容器；同目录新建容器。核对全部学生/照片版本/参数/审核/任务/交付/回收时间，11个非索引文件SHA-256一致。启动审计事件和修订号单独排除，不隐藏业务差异。
7. **自动清理通过**：仅在停止的测试副本上设置受控过期时间；禁用时重建后仍保留原时间，启用后再次新建容器，启动扫描完成补清理；正常学生、成片、审核和交付记录不变。中断使用文件删除异常注入，不宣称真实断电测试。
8. **旧版本边界验证**：旧镜像rc.3 ID sha256:5e23b99e1f172cdd39197dafbb4032dac14630ac832c7940a54ed6b353110ec0实际启动并建立测试名单；停止后将同目录给rc.4，程序按设计拒绝，所有旧文件哈希不变。**这是兼容保护验证，不是升级成功。** schema3不支持旧工作区迁移。
9. **GHCR复核通过**：用新建空Docker配置目录匿名pull，公开包可访问；从registry digest启动独立测试实例，健康、实际版本/提交、正常登录/退出、3名测试学生读取、回收站和交付下载通过。使用本地缓存层不影响registry manifest/digest校验。

## 修复及测试失败记录

发现并修复：单人弹窗读取已保存配置；跨届恢复任务的并发入口；接收时手动上传保护；设置保存中标签删除竞态；试处理文件纳入回收；永久清理重试重新核对共享引用；失败审计不得改变系统设置版本。最终镜像复测通过。

测试脚本曾定位到Element Plus隐藏input、同名照片标题或过渡动画，并有一次错误下载按钮名称；修正定位后重跑。ZIP原有manifest.json属正常文件，照片数量单独核对。首次服务启动尚未就绪时健康/浏览器请求失败，待真实健康就绪后重试通过。Git推送曾出现TLS握手错误，保持证书验证重试后成功。以上不计作外部阻塞。

## 未验证与限制

- 本轮本地快速处理/合成素材通过，不代表真实Hivision抠图、证件照合规性或人工视觉质量通过；未重复处理此前生产样本。
- 未测试ARM64、多副本、长时压力或真实断电；文件存储只支持单实例，禁止共享目录多副本。
- 新结构不支持rc.3及旧工作区；必须保留旧目录，使用新的data-rc4。没有迁移功能，不把换回旧镜像当数据回退。
- Excel/ZIP解析边界、本次参数HTTP重试和活动任务删除保护主要为接口验证，并非都做过浏览器错误场景。
- 本轮未重新验收GitHub在线更新检查，不上传Windows新包。仅发布候选，不更新stable/latest，不合并main，不修改生产部署。

## 截图索引

截图均来自同一最终镜像，无真实学生信息：21-system-settings（标签/回收站）、22-students、23-single-comparison（原图/成片）、24-single-delivery-history（阶段与交付）、25-deliveries、26-recycle-impact、27-login、28-ghcr-deliveries、29-ghcr-settings。早期过渡动画截图不作为展示附件。详细清单见FEATURE_ACCEPTANCE.md。
