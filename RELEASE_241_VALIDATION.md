# StudentPhotoFlow 2.4.1-rc.2 验收记录

日期：2026-09-16。基线：正式 v2.4.0。最终应用源码：`9b7d3c077814b376ea1802834103e71a7d5b60a4`；Git 标签 `v2.4.1-rc.2` 指向该提交。
镜像：`ghcr.io/hanknovic/studentphotoflow:2.4.1-rc.2`。
Registry digest：`sha256:d834c4c655491ff3e80783e02fd6f28f76e85bda843df3fe9a778f7ddff9466e`。此值来自 GHCR，不是本地 Image ID。
原始本地最终镜像上传 GHCR，没有在 Actions 中另行重建。后续验收文档提交不改变镜像源码或标签。

## 状态与方法

下表“通过”是已执行的验证；代码实现、接口验证、浏览器验证分别记录。所有工作区均为独立测试目录，照片为合成素材，不连接生产部署、不调用生产 Hivision。实际独立 Chromium 访问 Docker 容器页面，通过正常登录获取会话。模拟服务使用真实 HTTP 请求和可控延迟/错误，证据中保存开始、结束和峰值记录。

| 项目 | 代码 | 接口/容器验证 | 实际浏览器验证 |
|---|---|---|---|
| 开始、结束、总历时、运行时长 | 已实现 | 通过：自然完成后固定；运行区间不累加各照片耗时 | 通过：运行动态更新、暂停冻结、结束后不增长 |
| 暂停、继续 | 已实现 | 通过：2个在途请求收尾后暂停，继续仅处理剩余2项 | 通过：暂停→刷新→继续；恰好4次模拟调用 |
| 安全结束 | 已实现 | 通过：2成功、2未执行；继续接口409；重启保持终态 | 通过：并发结束说明、收尾、无继续按钮 |
| 异常中断计时 | 已实现 | 通过：实际kill/remove/recreate；已完成照片/版本保留，停机不计运行时长 | 通过：恢复后的计时页面；精度限制见下文 |
| 历史及统一任务计时 | 已实现 | 通过：缺字段兼容，接收/打包也持久化 | 通过：旧任务显示未记录，接收和打包任务有时间 |
| 请求并发设置1–16 | 已实现 | 通过：整数范围校验；缺字段默认1 | 通过：保存、刷新、导出2、旧JSON导入默认1、再次保存2 |
| 实际并发及共享限额 | 已实现 | 通过：串行峰值1；跨届任务峰值3；降低至1后后续调度≤1；单张试处理共用限额 | 通过：单人补录与另一届批量共同峰值2 |
| 错误与名额释放 | 已实现 | 通过：HTTP503、非法JSON、超时均结束，其他成功结果保留，名额释放 | 已有错误区域；具体故障组合按接口验收记录 |
| 重复提交/结果保护 | 已实现 | 通过：相同结果计划跳过；手动终态不可继续 | 通过：关闭单人弹窗后继续执行，重新打开看到结果 |
| 业务回归 | 复用原流程 | 通过：照片按学号正确写回，下载包4张可读JPG | 通过：ZIP预检→确认→计划→处理→审核→生成包→下载→确认 |
| v2.4.0有数据升级 | 兼容增量字段 | 通过：323学生、10任务、6交付、1回收站及业务字段/照片SHA全部保留 | 通过：升级后读取任务、配置、交付 |
| 容器重建持久化 | 已实现 | 通过：128文件校验值及全部业务字段一致，配置并发2保留 | 通过：GHCR拉取镜像登录、版本、计时、旧交付包下载及退出 |
| GHCR产物 | 已上传 | 通过：隔离无登录Docker配置匿名拉取指定digest，健康及源码版本一致 | 通过：拉取产物实际页面检查 |

## 数值证据

- 串行配置1：峰值1、2次调用；共享配置3：峰值3、共6次调用。运行时下调1，现有请求收尾后新请求串行。
- 暂停/继续：4项、4次调用，实际运行4.421984秒，总历时约5.361099秒。
- 手动结束：运行2.223245秒，4项中2成功、2未执行；结束时间在结果保存之后。
- 异常退出：检查点确认3.082370秒；恢复并处理剩余项目后累计5.431753秒，总历时约10.546087秒，明确标记确认下限。不确定的在途项由测试明确选择跳过，不自动重试。
- 从GHCR匿名拉取后，另做暂停重建：暂停累计1.869305秒，恢复完成累计3.726697秒；总历时约9.298429秒，3项恰好3次调用。
- UI单人和批量共同服务：峰值2；关闭弹窗不中止任务；批量手动结束后剩余2人不执行。
- 实际交付下载：`00101.jpg`、`00102.jpg`、`00103.jpg`、`00104.jpg`；数量4，JPG均可读取，前导零保留。

## 测试与证据文件

最终镜像直接执行核心测试38项通过（test_rc5、test_system、test_v2、test_task_timing），未覆盖应用源码。另有此前源码挂载环境59项流水线/业务/新功能测试通过，此项不替代最终镜像验收。广泛桌面套件未全通过：Linux无Tk，最终Docker镜像也不包含Windows review_web导出器；相关桌面测试不作为本次Linux验收通过项。

`acceptance-evidence.json`收录脱敏后的合成HTTP统计、升级数量、浏览器断言、重建哈希比较、下载检查及registry验证。
`screenshots.zip`内01配置、02运行、03正在暂停、04已暂停、05完成、06交付、07手动结束、08历史未记录、09单人共享并发、10并发安全结束、11单人结果、12匿名拉取镜像计时。截图均为同一最终源码/镜像的真实浏览器截图，仅合成学生及素材。

## 已知限制 / 未验证

- 未测试真实Hivision性能及人像视觉质量；模拟峰值不代表提高并发一定提速。本轮未重复调用此前成功处理的真实照片。
- 崩溃后只累计已落盘检查点，最后心跳之后的尾段可能漏记，展示已确认下限。旧任务无可靠记录显示未记录；恢复旧任务只提供恢复后的部分运行时长。
- 限额作用于同一应用进程的同一规范化服务地址；保持单进程单副本及同届次一个正式任务。不同URL别名不会自动识别为同一物理服务。
- 本地HTTP超时不能取消外部服务已开始的后台计算；不进行自动重试。
- 实测Linux amd64；Windows桌面、其他架构未验收。
- 继承2.4.0的更新检查限制：网页仅查询正式Release，环境变量rc不改变这一行为。候选版本请按固定镜像标签升级。
- rc.1标签/镜像为曾过早推送的不完整中间点，保留历史、不创建其Release；本次交付完整验收的rc.2。

## 升级与数据安全

从2.4.0沿用原Compose项目名、挂载路径、端口、口令。先暂停并等在途收尾，停止服务，完整备份数据和.env，仅修改SPF_IMAGE；执行显式`--env-file .env`的pull/up。不要覆盖原.env，不清空目录，不使用down -v。
任务计时是兼容的增量字段；首次打开保存before-task-timing-1.json索引备份，完整照片仍需整个数据目录备份。失败报错而不是重置数据。回退先保留新目录，再从升级前备份恢复到另一目录并使用旧镜像；换旧镜像本身不等于数据回退。完整命令见部署ZIP内DOCKER部署.md。

## 发布后核对

已创建 https://github.com/HankNovic/StudentPhotoFlow/releases/tag/v2.4.1-rc.2 ，draft=false、prerelease=true。8个上传附件与GitHub记录的SHA-256一致，部署ZIP回下载逐字节一致，Compose解析后的固定镜像、内部8769端口和/data挂载正确。GitHub最新正式版本仍为v2.4.0；远程main仍为37797a93c901ebc8f8c67fb8df25324332940ec8。2.4.0、stable、latest均保持sha256:5a394a3d4d1fd5b49a09ed1ffa0cc676d3c3275c45fdc2fb11d0920ec3f8c792。未修改生产部署。
